export default function printErrorMsg(errorMsg){
    let errorDiv = document.getElementById('errorDiv');
    errorDiv.innerHTML = errorMsg;
    //Not sure if this should be more advanced
    //Maybe a "popup"
}